describe("streama module", function() {

    beforeEach(angular.mock.module("streama", function() {
    }));

    describe("kbSubscription-serviceService", function() {

        var kbSubscription-serviceService;

        beforeEach(angular.mock.inject(function(_kbSubscription-serviceService_) {
            kbSubscription-serviceService = _kbSubscription-serviceService_;
        }));

        it("should be tested", function() {
            expect(true).toEqual(false);
        });

    });

});
